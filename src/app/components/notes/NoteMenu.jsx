import React, { PropTypes } from 'react';

const NoteMenu = () => (
  <div id="menu">
    <div className="h2 w-100 bb b--near-white" />
  </div>
);

NoteMenu.propTypes = {
};

export default NoteMenu;
